[package]
name = "parquet_mcp_server"
version = "0.1.0"
edition = "2021"

[dependencies]
datafusion = "55.0.0"
rmcp = { version = "3.3.0", features = ["server"] }
tokio = { version = "1.0", features = ["rt-multi-thread"] }

